#include  <stdio.h>
main( )
{
	double a,x0,x1;
	printf("输入一个实数, 本程序计算并输出其平方根\n");
	do
/**********************found***********************/
	scanf("%lf",&a);
	while(a<=0);
	x1=a/2.0;
	do
	{
/**********************found***********************/
		x0=x1;
		x1=(x0+a/x0)/2.0;
/**********************found***********************/
	}while((x1-x0)>1e-6  ||x1-x0<-1e-6  );
	printf("%f 的平方根是 %f\n",a,x1);
}